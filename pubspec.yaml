name: pideqr
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  cloud_firestore: ^5.1.0
  firebase_auth: ^5.1.2
  firebase_core: ^3.2.0

  # Manejo de Estado
  flutter_riverpod: ^2.5.1

  # Utilidades de UI y QR
  mobile_scanner: ^5.1.1
  qr_flutter: ^4.1.0
  dynamic_color: ^1.8.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

  # --- NUEVAS DEPENDENCIAS PARA IMPRESIÓN ---
  printing: ^5.12.0
  pdf: ^3.10.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
